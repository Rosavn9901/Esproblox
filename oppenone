-- TRÌNH QUẢN LÝ TACTICAL & CHARACTER ESP (VỚI NÚT ĐÓNG/MỞ GUI)
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ToggleBtn = Instance.new("TextButton")
local StatusLab = Instance.new("TextLabel")
local OpenCloseBtn = Instance.new("TextButton") -- Nút đóng/mở GUI

-- 1. Thiết lập ScreenGui
ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "CombinedESP_UI"
ScreenGui.ResetOnSpawn = false

-- 2. Nút nhỏ để hiện/ẩn Menu (Nằm ở góc dưới bên trái)
OpenCloseBtn.Name = "OpenCloseBtn"
OpenCloseBtn.Parent = ScreenGui
OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
OpenCloseBtn.Position = UDim2.new(0.02, 0, 0.9, 0)
OpenCloseBtn.Size = UDim2.new(0, 80, 0, 30)
OpenCloseBtn.Text = "HIỆN MENU"
OpenCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenCloseBtn.Font = Enum.Font.GothamBold
OpenCloseBtn.TextSize = 12
Instance.new("UICorner", OpenCloseBtn).CornerRadius = UDim.new(0, 6)

-- 3. Menu Chính (MainFrame)
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.Position = UDim2.new(0.05, 0, 0.4, 0)
MainFrame.Size = UDim2.new(0, 200, 0, 120)
MainFrame.Active = true
MainFrame.Draggable = true 
MainFrame.Visible = true -- Mặc định hiện khi chạy script

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "Tactical & NPC ESP"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16

StatusLab.Parent = MainFrame
StatusLab.Position = UDim2.new(0, 0, 0.3, 0)
StatusLab.Size = UDim2.new(1, 0, 0, 20)
StatusLab.Text = "Trạng thái: TẮT"
StatusLab.TextColor3 = Color3.fromRGB(255, 0, 0)
StatusLab.BackgroundTransparency = 1
StatusLab.Font = Enum.Font.Gotham
StatusLab.TextSize = 14

ToggleBtn.Parent = MainFrame
ToggleBtn.Position = UDim2.new(0.1, 0, 0.55, 0)
ToggleBtn.Size = UDim2.new(0.8, 0, 0, 35)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ToggleBtn.Text = "BẬT ESP TỔNG"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamSemibold
ToggleBtn.TextSize = 14
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 6)

---------------------------------------------------------
-- LOGIC ĐÓNG/MỞ GIAO DIỆN
---------------------------------------------------------
OpenCloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    if MainFrame.Visible then
        OpenCloseBtn.Text = "ẨN MENU"
        OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    else
        OpenCloseBtn.Text = "HIỆN MENU"
        OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    end
end)

---------------------------------------------------------
-- LOGIC ESP HỢP NHẤT
---------------------------------------------------------
local Workspace = game:GetService("Workspace")
local ESPEnabled = false
local espConnection = nil

local tacticalList = {
    ["SignalDisruptor"] = Color3.fromRGB(0, 255, 255),
    ["ProximityAlarm"]  = Color3.fromRGB(255, 165, 0),
    ["FragGrenade"]     = Color3.fromRGB(255, 0, 0),
    ["Remotec4"]        = Color3.fromRGB(255, 0, 0),
    ["Claymore"]        = Color3.fromRGB(255, 0, 0),
    ["Bomb"]            = Color3.fromRGB(255, 0, 0),
    ["Drone"]           = Color3.fromRGB(0, 255, 0),
    ["StickyCamera"]    = Color3.fromRGB(255, 255, 0)
}

local function applyHighlight(instance, color, name)
    if not instance:FindFirstChild(name) then
        local highlight = Instance.new("Highlight")
        highlight.Name = name
        highlight.Parent = instance
        highlight.FillColor = color
        highlight.FillTransparency = 0.5
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.OutlineTransparency = 0
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    end
end

local function checkAndApply(obj)
    if not ESPEnabled then return end
    if tacticalList[obj.Name] then
        applyHighlight(obj, tacticalList[obj.Name], "ESP_Highlight")
    end
    if obj:IsA("Model") then
        if obj:FindFirstChild("torso") or obj:FindFirstChild("head") or obj:FindFirstChild("arm1") then
            applyHighlight(obj, Color3.fromRGB(255, 0, 0), "ESP_Highlight")
        end
    end
end

local function removeAllESP()
    for _, obj in pairs(Workspace:GetDescendants()) do
        local hl = obj:FindFirstChild("ESP_Highlight")
        if hl then hl:Destroy() end
    end
end

ToggleBtn.MouseButton1Click:Connect(function()
    ESPEnabled = not ESPEnabled
    if ESPEnabled then
        ToggleBtn.Text = "TẮT ESP"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        StatusLab.Text = "Trạng thái: ĐANG BẬT"
        StatusLab.TextColor3 = Color3.fromRGB(0, 255, 0)
        for _, obj in pairs(Workspace:GetDescendants()) do checkAndApply(obj) end
        espConnection = Workspace.DescendantAdded:Connect(function(newObj)
            task.wait(0.5)
            checkAndApply(newObj)
        end)
    else
        ToggleBtn.Text = "BẬT ESP TỔNG"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
        StatusLab.Text = "Trạng thái: TẮT"
        StatusLab.TextColor3 = Color3.fromRGB(255, 0, 0)
        if espConnection then espConnection:Disconnect() espConnection = nil end
        removeAllESP()
    end
end)

print("ESP Loaded with Open/Close Button!")
